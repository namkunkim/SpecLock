# 기본은 텍스트로 취급하고 체크아웃 시 OS에 맞춤
* text=auto

# Windows 스크립트는 항상 CRLF (배치 파일은 LF면 오작동할 수 있다)
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Unix 스크립트는 항상 LF
*.sh  text eol=lf

# 문서
*.md  text eol=lf
*.yml text eol=lf
*.kt  text eol=lf
